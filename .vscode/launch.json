{
    "configurations": [
        {
            "name": "System QEMU Debug",
            "type": "gdb",
            "request": "attach",
            "executable": "symbols/kernel32.sym",
            "target": "localhost:1234",
            "remote": true,
            "cwd": "${workspaceRoot}", 
            "gdbpath": "gdb",
        }
    ],
}